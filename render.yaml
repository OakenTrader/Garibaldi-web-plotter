# Exported from Render on 2025-09-02T21:09:29Z
services:
- type: web
  name: Garibaldi-web-plotter
  runtime: python
  repo: https://github.com/OakenTrader/Garibaldi-web-plotter
  branch: garibaldi-render
  plan: free
  envVars:
  - key: CAMPAIGN_NAME
    sync: false
  - key: FILE_ID
    sync: false
  region: oregon
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:server --bind 0.0.0.0:$PORT
  autoDeployTrigger: commit
version: "1"
